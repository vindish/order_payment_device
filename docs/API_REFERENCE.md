# API Reference

Base URL for the aggregate API:

```text
http://localhost:8000/api/v1
```

Interactive OpenAPI docs:

```text
http://localhost:8000/docs
```

## System

### GET /health

No auth.

Response:

```json
{
  "status": "ok",
  "service": "Order Payment Device",
  "env": "prod"
}
```

## Auth

### POST /auth/login

Request:

```json
{
  "username": "admin",
  "password": "123456"
}
```

Response:

```json
{
  "access_token": "...",
  "refresh_token": "...",
  "token_type": "bearer"
}
```

### POST /auth/refresh

Request:

```json
{
  "refresh_token": "..."
}
```

### POST /auth/logout

Request:

```json
{
  "refresh_token": "..."
}
```

## Users

### GET /users/me

Auth: Bearer JWT.

Response:

```json
{
  "id": 1,
  "username": "admin",
  "is_active": true,
  "role": "admin"
}
```

## Devices

### POST /devices

Auth: Bearer JWT with role `operator` or above.

Request:

```json
{
  "sn": "LOCK-001",
  "name": "测试门锁"
}
```

Response includes `device_secret`. Store it immediately because only its hash is persisted.

```json
{
  "id": 1,
  "sn": "LOCK-001",
  "name": "测试门锁",
  "status": "offline",
  "last_seen_at": null,
  "device_secret": "..."
}
```

### GET /devices

Auth: Bearer JWT with role `operator` or above.

### POST /devices/{sn}/heartbeat

Auth: device headers.

Headers:

```text
X-Device-SN: LOCK-001
X-Device-Token: <device_secret>
```

Marks the device `online` and updates `last_seen_at`.

### POST /devices/events/unlocked?order_id={id}

Auth: device headers.

Marks an order as `DONE`. The order must currently be in a state that can transfer to `DONE`.

### GET /devices/{sn}/shadow

Auth: Bearer JWT.

Returns desired/reported device shadow state.

### PUT /devices/{sn}/shadow

Auth: Bearer JWT with role `operator` or above.

Request:

```json
{
  "desired_state": {
    "lock": "open"
  },
  "reported_state": {
    "battery": 87
  }
}
```

### POST /devices/{sn}/commands

Auth: Bearer JWT with role `operator` or above.

Request:

```json
{
  "command": "unlock",
  "payload": {
    "order_id": 1
  },
  "idempotency_key": "unlock:1"
}
```

Response:

```json
{
  "id": 1,
  "status": "SENT"
}
```

### POST /devices/commands/ack

Auth: device headers.

Request:

```json
{
  "command_id": 1,
  "status": "ACKED",
  "error_message": null
}
```

### POST /devices/telemetry

Auth: device headers.

Request:

```json
{
  "metric": "battery",
  "value": "87.000000",
  "unit": "%",
  "recorded_at": "2026-05-13T12:00:00"
}
```

## Orders

### POST /orders

Auth: Bearer JWT.

Optional header:

```text
Idempotency-Key: create-order-LOCK-001-001
```

Request:

```json
{
  "device_sn": "LOCK-001",
  "amount": "1.00"
}
```

Response:

```json
{
  "id": 1,
  "user_id": 1,
  "device_id": 1,
  "amount": "1.00",
  "currency": "CNY",
  "status": "INIT",
  "retry_count": 0,
  "created_at": "2026-05-13T12:00:00"
}
```

### GET /orders

Auth: Bearer JWT.

Returns orders for the current user.

## Payments

### POST /payments/callback

No user JWT. Uses payment signature or callback token.

Optional header:

```text
Idempotency-Key: payment-order-1
```

Request:

```json
{
  "order_id": 1,
  "provider": "manual",
  "trade_no": "manual-001",
  "status": "PAID",
  "token": "replace-with-provider-secret",
  "signature": null
}
```

Only `PAID` callbacks are currently supported.

Response:

```json
{
  "msg": "Payment accepted",
  "order_id": 1,
  "status": "PAID"
}
```

Duplicate callbacks for already paid/unlocking/done orders are accepted and ignored.

## Payment Signature Payload

Canonical payload is generated by `backend/app/core/payment_signing.py`:

```json
{
  "order_id": 1,
  "provider": "manual",
  "status": "PAID",
  "trade_no": "manual-001"
}
```

The JSON is serialized with sorted keys and compact separators before HMAC verification. See `backend/app/core/security.py` for the generic signature helper.
